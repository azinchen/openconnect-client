name: Maintenance - Automated Updates

permissions:
  contents: write
  pull-requests: write

on:
  schedule:
    # Run at 3 AM UTC daily to stagger the updates
    - cron: '0 3 * * *'
  workflow_dispatch:
    inputs:
      update_type:
        description: 'Type of update to run'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - packages
          - apk
          - alpine
      force_update:
        description: 'Force update MD files even if content unchanged'
        required: false
        default: false
        type: boolean

jobs:
  update-apk-packages:
    name: 📦 Update APK Packages
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.update_type == 'all' || github.event.inputs.update_type == 'apk' || github.event.inputs.update_type == '' }}
    
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Update APK package versions
        id: update-apk
        run: |
          chmod +x ./scripts/update-apk-versions.sh
          ./scripts/update-apk-versions.sh Dockerfile

          UPDATE_DATE=$(date +%y%m%d)
          echo "update_date=${UPDATE_DATE}" >> $GITHUB_ENV
          echo "update_date=${UPDATE_DATE}" >> $GITHUB_OUTPUT

      - name: Create Pull Request for APK updates
        uses: peter-evans/create-pull-request@v8.1.1
        with:
          token: ${{ secrets.PR_PAT }}
          branch: bot/update-apk-packages
          commit-message: "deps: update ${{ steps.update-apk.outputs.updated_count }} APK package(s) (${{ env.update_date }})"
          delete-branch: true
          title: "📦 Update APK packages - ${{ env.update_date }}"
          body: |
            ## 📦 APK Package Updates
            
            **Date**: ${{ env.update_date }}
            **Packages updated**: ${{ steps.update-apk.outputs.updated_count }} of ${{ steps.update-apk.outputs.total_packages }} checked
            
            ### Updated Packages:
            ${{ steps.update-apk.outputs.packages_updated }}
            
            ### Changes:
            - Updated Alpine Linux package versions in Dockerfile
            - Maintained compatibility with existing configurations
            
            ---
            🤖 Auto-generated by [update-apk workflow](https://github.com/${{ github.repository }}/actions/workflows/maintenance-updates.yml)

  update-github-packages:
    name: 📋 Update GitHub Package Versions
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.update_type == 'all' || github.event.inputs.update_type == 'packages' || github.event.inputs.update_type == '' }}
    strategy:
      matrix:
        repository: ["just-containers/s6-overlay"]
    
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Get latest version for ${{ matrix.repository }}
        id: get-version
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Authenticate to avoid unauthenticated rate limits that return non-JSON
          RESPONSE=$(curl -fsSL \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${{ matrix.repository }}/tags")
          TAG=$(echo "$RESPONSE" | jq -r ".[0].name")
          if [ -z "$TAG" ] || [ "$TAG" = "null" ]; then
            echo "Failed to determine latest tag for ${{ matrix.repository }}. Response was:" >&2
            echo "$RESPONSE" | head -c 500 >&2
            exit 1
          fi
          PACKAGE_VERSION=${TAG:1}
          BRANCH_NAME="bot/update-$(echo '${{ matrix.repository }}' | sed 's/\//-/g')-${PACKAGE_VERSION}"

          echo "package_version=${PACKAGE_VERSION}" >> $GITHUB_ENV
          echo "branch_name=${BRANCH_NAME}" >> $GITHUB_ENV
          echo "repo_name=$(echo '${{ matrix.repository }}' | cut -d'/' -f2)" >> $GITHUB_ENV

      - name: Update Dockerfile with new version
        shell: bash
        run: |
          ESCAPED_SEARCH=$(printf '%s\n' "${{ matrix.repository }}" | sed -e 's/[\/&]/\\&/g')
          sed -i "/.*${ESCAPED_SEARCH}.*/{n;s/.*/ARG PACKAGEVERSION=\"${{ env.package_version }}\"/}" Dockerfile

      - name: Update README.md with new version
        shell: bash
        run: |
          echo "Updating ${{ env.repo_name }} version in README.md to ${{ env.package_version }}..."
          sed -i -E "s/(\*\*Init\*\*: s6-overlay )[^ ]*/\1${{ env.package_version }}/" README.md

      - name: Create Pull Request for ${{ matrix.repository }}
        uses: peter-evans/create-pull-request@v8.1.1
        with:
          token: ${{ secrets.PR_PAT }}
          branch: bot/${{ env.branch_name }}
          commit-message: "deps: bump ${{ env.repo_name }} to v${{ env.package_version }}"
          delete-branch: true
          title: "📋 Bump ${{ env.repo_name }} to v${{ env.package_version }}"
          body: |
            ## 📋 Package Version Update
            
            **Repository**: [${{ matrix.repository }}](https://github.com/${{ matrix.repository }})
            **Version**: `${{ env.package_version }}`
            
            This PR updates the package version in the Dockerfile.
            
            ### Changes:
            - Updated `PACKAGEVERSION` environment variable in Dockerfile
            - Updated version reference in README.md
            - Bumped from previous version to `${{ env.package_version }}`
            
            ---
            🤖 Auto-generated by [update-packages workflow](https://github.com/${{ github.repository }}/actions/workflows/maintenance-updates.yml)

  update-openconnect-version:
    name: 🔄 Update OpenConnect Version
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.update_type == 'all' || github.event.inputs.update_type == 'packages' || github.event.inputs.update_type == '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Get latest openconnect version
        id: get-version
        shell: bash
        run: |
          # Latest stable release tag (vX.YY) from the upstream GitLab project
          TAG=$(curl -Ls "https://gitlab.com/api/v4/projects/openconnect%2Fopenconnect/repository/tags" \
            | jq -r '[.[] | select(.name | test("^v[0-9]+\\.[0-9]+$"))][0].name')
          if [ -z "$TAG" ] || [ "$TAG" = "null" ]; then
            echo "Failed to determine latest openconnect tag" >&2
            exit 1
          fi
          OPENCONNECT_VERSION=${TAG#v}
          BRANCH_NAME="bot/update-openconnect-version"

          echo "openconnect_version=${OPENCONNECT_VERSION}" >> $GITHUB_OUTPUT
          echo "branch_name=${BRANCH_NAME}" >> $GITHUB_OUTPUT

      - name: Update Dockerfile with new openconnect version
        shell: bash
        run: |
          # Single top-level ARG; the stages inherit it via bare ARG lines
          sed -i "s/OPENCONNECT_VERSION=[0-9][0-9.]*/OPENCONNECT_VERSION=${{ steps.get-version.outputs.openconnect_version }}/" Dockerfile

      - name: Create Pull Request for openconnect update
        uses: peter-evans/create-pull-request@v8.1.1
        with:
          token: ${{ secrets.PR_PAT }}
          branch: ${{ steps.get-version.outputs.branch_name }}
          commit-message: "deps: bump openconnect to v${{ steps.get-version.outputs.openconnect_version }}"
          delete-branch: true
          title: "🔄 Bump openconnect to v${{ steps.get-version.outputs.openconnect_version }}"
          labels: |
            dependencies
          body: |
            ## 🔄 OpenConnect Version Update

            **Repository**: [openconnect/openconnect](https://gitlab.com/openconnect/openconnect)
            **Version**: `${{ steps.get-version.outputs.openconnect_version }}`

            This PR updates the openconnect version built into the image.

            ### Changes:
            - Updated the top-level `OPENCONNECT_VERSION` ARG in Dockerfile
            - Upstream changelog: https://www.infradead.org/openconnect/changelog.html

            ---
            🤖 Auto-generated by [maintenance-updates workflow](https://github.com/${{ github.repository }}/actions/workflows/maintenance-updates.yml)

  update-alpine-version:
    name: 🐧 Update Alpine Version
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.update_type == 'all' || github.event.inputs.update_type == 'alpine' || github.event.inputs.update_type == '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Determine latest Alpine version
        id: get-alpine
        shell: bash
        run: |
          echo "Fetching latest Alpine tags from Docker Hub..."
          # Fetch tags and select the highest semantic version (major.minor.patch)
          TAGS=$(curl -s 'https://registry.hub.docker.com/v2/repositories/library/alpine/tags?page_size=200' \
            | jq -r '.results[].name' \
            | grep -E '^[0-9]+\.[0-9]+(\.[0-9]+)?$' \
            | sed -E 's/^([0-9]+\.[0-9]+)$/\1.0/' \
            | sort -V)

          if [ -z "$TAGS" ]; then
            echo "Failed to fetch Alpine tags" >&2
            exit 1
          fi

          LATEST=$(echo "$TAGS" | tail -n1)
          echo "Latest Alpine version: $LATEST"

          # Capture current version from the ALPINE_VERSION ARG before change
          CURRENT=$(grep -E '^ARG[[:space:]]+ALPINE_VERSION=' Dockerfile | head -n1 | sed -E 's/.*ALPINE_VERSION=([^[:space:]]+).*/\1/')
          echo "Current Alpine version in Dockerfile: $CURRENT"

          echo "latest=$LATEST" >> $GITHUB_OUTPUT
          echo "current=$CURRENT" >> $GITHUB_OUTPUT
          DATE=$(date +%y%m%d)
          echo "update_date=${DATE}" >> $GITHUB_ENV

      - name: Update Dockerfile if newer version available
        id: update-dockerfile
        if: ${{ steps.get-alpine.outputs.latest != '' && steps.get-alpine.outputs.latest != steps.get-alpine.outputs.current }}
        shell: bash
        run: |
          LATEST='${{ steps.get-alpine.outputs.latest }}'
          echo "Updating ALPINE_VERSION ARG to ${LATEST}..."
          # Update the single ALPINE_VERSION ARG; all FROM alpine:${ALPINE_VERSION} lines inherit it
          sed -i -E "s/^(ARG[[:space:]]+ALPINE_VERSION=).*/\1${LATEST}/" Dockerfile
          echo "updated=true" >> $GITHUB_OUTPUT
          echo "new_version=${LATEST}" >> $GITHUB_OUTPUT

      - name: Update APK package versions
        id: update-apk
        if: steps.update-dockerfile.outputs.updated == 'true'
        shell: bash
        run: |
          echo "Updating APK package versions for new Alpine base..."
          chmod +x ./scripts/update-apk-versions.sh
          ./scripts/update-apk-versions.sh Dockerfile

      - name: Update README.md with new Alpine version
        if: steps.update-dockerfile.outputs.updated == 'true'
        shell: bash
        run: |
          LATEST='${{ steps.get-alpine.outputs.latest }}'
          echo "Updating Alpine version in README.md to ${LATEST}..."
          sed -i -E "s/(\*\*Base\*\*: Alpine Linux )[^ ]*/\1${LATEST}/" README.md

      - name: Create Pull Request for Alpine bump
        if: steps.update-dockerfile.outputs.updated == 'true'
        uses: peter-evans/create-pull-request@v8.1.1
        with:
          token: ${{ secrets.PR_PAT }}
          branch: bot/update-alpine
          commit-message: "deps: bump Alpine to v${{ steps.get-alpine.outputs.latest }} (${{ env.update_date }})"
          delete-branch: true
          title: "🐧 Bump Alpine to v${{ steps.get-alpine.outputs.latest }} - ${{ env.update_date }}"
          body: |
            ## 🐧 Alpine Base Image Update

            **File**: `Dockerfile`
            **Date**: ${{ env.update_date }}
            **Source**: Docker Hub `library/alpine` tags

            ### Change
            - Current: `${{ steps.get-alpine.outputs.current }}`
            - New: `${{ steps.get-alpine.outputs.latest }}`

            This PR updates the `ALPINE_VERSION` ARG to the latest stable Alpine tag (inherited by all `FROM alpine:${ALPINE_VERSION}` stages) and updates version references in README.md.

            ---
            🤖 Auto-generated by [update-alpine workflow](https://github.com/${{ github.repository }}/actions/workflows/maintenance-updates.yml)
